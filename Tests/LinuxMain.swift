import XCTest
@testable import LoggerTests

// Test do not run on Linux yet because of
// Fatal error: url(for:in:appropriateFor:create:) is not yet implemented:
// file Foundation/FileManager.swift, line 78
XCTMain([
    //testCase(LoggerTests.allTests),
])
